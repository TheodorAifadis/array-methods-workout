// Includes
// Metoden talar om ifall en array innehåller ett visst element
// Sedan returneras true eller false beroende på om den innehåller elementet eller inte

const sports = ['fotboll', 'basket', 'bandy', 'hockey', 'handboll']

console.log(sports.includes('basket')) // borde ge true
console.log(sports.includes('volleyboll')) // borde ge false
